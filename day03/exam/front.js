const axios = require('axios');
const url = 'http://localhost:3000/board';

class BoardFront{
    async createForm(){
        const board = {}
        board.title = this.
        
    }
}

const board = new BoardFornt();

board();